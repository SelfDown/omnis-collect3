service:
  - key: get_current_user_menu
    module: sql
    params:
      user_id:
        template: "{{session_user_id}}"
    sql_file: menu.sql
    result_handler:
      - key: to_tree
        params:
          id_field: menu_id
          parent_id_field: parent_id
          children_field: children